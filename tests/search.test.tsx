import { render, screen } from '@testing-library/react';
import { Search } from '../src/components/Search/Search';


describe.todo('App', () => {
  it.todo('renders headline', () => {
    render(<Search />);

    screen.debug();

    // check if App components renders headline
  });
});